{
  "name": "js-todo",
  "version": "1.0.0",
  "description": "In-memory todo list — E2E fixture for claude-architect",
  "main": "src/todo.js",
  "scripts": {
    "test": "jest --testPathPattern='tests/' --verbose"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
