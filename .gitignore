# Ignore Claude Code runtime session state (skills/commands/settings.json are committed)
claude.log
.task_handoff.md
GEMINI_ADVICE.md
gemini_advice_for_testing_and_architect_and_debugging.md

# Ignore OAuth credentials — never commit these
claude-auth/

# Local environment overrides — never commit
.env.local

# Ignore local OS or editor artifacts
.DS_Store
.idea/
.vscode/
*.swp
