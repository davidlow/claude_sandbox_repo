---
description: Manage docs/decisions/ decision log files. Actions: init (create log), section (append file content), note (append inline text), outcome (finalize status), read (show recent logs).
argument-hint: "[init|section|note|progress|outcome|read] <pipeline> <task/args...>"
---

Use the /logging skill to complete this request: $ARGUMENTS
