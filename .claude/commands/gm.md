---
description: General manager — reads tasks.md (or a prompt), branches per task, invokes the right pipeline skill, optionally runs a QA layer, and merges to base only on full success.
argument-hint: "[--tasks <file>] [--qa] [--no-gemini] [<prompt or task list>]"
---
Use the /gm skill to complete this request: $ARGUMENTS
