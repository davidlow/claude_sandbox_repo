This is a rough list of tasks to be completed.

(1) The docs folder is not well organized.  Essentially, I want to turn the docs folder in to a full fledged wiki in markdown so I can evaluate the changes both live and after-the-fact.  Future claude sessions should also be able to evaluate what was done efficiently.  First, I'd like all the md files to be organized first by my task (the one that I gave the orchestrator "/gm" or other orchestrator).  A folder named by the time/date that it was started and a rough summary of the ask.  Then, within that folder, I want to a current status / general overview that the "/gm" or whatever orchestrator skill updates and records the big picture goals.  Within this md file, I want links to other md files so its easy to follow (like each step, what skill was called, and a link to the md file that skill generated).  Within this folder, I want all the decision logs (in a decision folder) as well as all the architecture candidates.  I would prefer you add structure to this folder, either grouping the decisions together and architecture candidates, and any other type of logs in their own folder (implementation, qa, gemini requests / responses from the api) or you can group them by steps in the project.  Either way, I want all the md files to be accessible somehow from the "/gm" main orchestrator md file.  I don't want them all flat linked but some logical tree.  This gives us the ability to go into the task and fully track / follow what architectures were explored, what decisions were made, implementation details, how the testing was accomplished / built, what stages gemini was asked, etc.  Make sure to keep good track of at the original doc folder (for this task) what the current progress is and a enough information that if the claude instance / computer failed mid-edit, another claude instance could take over and understand what was done, what needs to be done, and how to do it without too much difficulty.  This might involve re-designing the logging element of this repo as well.

(2) Please analyze the recent change that was attempting to create a wiki-style documentation with live updates for
  repos where this claude-box is deployed.  It seems like there are a number of issues (1) the main docs folder
  still has the approved_architecture.md, architecture_candidates, and gemini_architectural_audits stored. (2) It
  doesn't seem like the inner task folders (docs/date_time_taskname) are actually being populated intelligently.
  There is just an overview.md file and a few emtpy folders.  I want all the documents stored inside this folder and
  detailed feedback about what is currently occuring at every step.  I should be able to open the overview.md file
  in some markdown viewer and find all of the architecture proposals, decisions, gemini feedback, the current status
  of the gm (is it implementing, is it in qa, what was the conclusion of the qa, etc etc).  Please see where the
  break in logic occured.  Please note that the previous docs were moved to legacy_docs/ if you want to read them.
